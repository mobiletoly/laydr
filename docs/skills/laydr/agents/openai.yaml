interface:
  display_name: "Laydr App"
  short_description: "Create and maintain Laydr apps"
  default_prompt: "Use $laydr-app to add or change Laydr routes, generated destinations, Compose hosts, Nav3 KMP or AndroidX Nav3 navigation, route-local workflows, and validation."
